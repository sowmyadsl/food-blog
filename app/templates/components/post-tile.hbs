<li>
  {{post.title}}
  <br>
  {{post.body}}
  <br>
  "{{post.category}}"
  {{#if isImageShowing}}
  <p><img src={{post.image}} alt={{post.category}} {{action 'imageHide'}}></p>
  {{else}}
  <button{{action 'imageShow'}}>Show image </button>
  {{/if}}
</li>
